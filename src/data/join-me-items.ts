export interface JoinMeItem {
  title: string;
  overview: string;
  requirements: string[];
  image: string;
  delivers?: string[];
}

/** Business hiring intro removed — kept empty intentionally. */
export const joinMeItems: JoinMeItem[] = [];
